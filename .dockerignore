# --- CÁC THƯ MỤC HỆ THỐNG & RÁC ---
.git
.gitignore
.dockerignore
Dockerfile
Jenkinsfile
README.md

# --- QUAN TRỌNG NHẤT: CHẶN NODE_MODULES Ở MÁY THẬT ---
# Nếu không chặn dòng này, lệnh "COPY . ." sẽ chép đè folder rỗng/lỗi 
# từ máy bạn vào container, làm mất folder đã cài bằng "npm ci"
node_modules
npm-debug.log

# --- CÁC FILE LOG & REPORT ---
coverage/
dependency-check-report.html
dependency-check-report.xml

# --- KHÔNG CHẶN *.json ---
# Xóa bỏ dòng *.json cũ để tránh rủi ro chặn nhầm file config
# Docker tự động hiểu package.json cần thiết nhờ lệnh COPY trong Dockerfile
